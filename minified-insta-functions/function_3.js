/*FB_PKG_DELIM*/

(_btldr = {}),
  (qpl_inl = (function () {
    var a = {};
    return function (b, c, d) {
      if (!b) return a;
      a[b] || (a[b] = {});
      a[b][c] = d || now_inl();
    };
  })()),
  (qpl_tag = (function () {
    var a = [];
    return function (b) {
      if (!b) return a;
      a.push(b);
    };
  })());
//# sourceURL=https://static.cdninstagram.com/rsrc.php/v3/yJ/r/575oEHuOly0.js?_nc_x=Ij3Wp8lg5Kz
// These functions and the empty object appear to be part of a mechanism for managing and storing data in some form. 
// qpl_inl seems to be used for setting values in an object, possibly for some kind of data tracking or recording. 
// qpl_tag appears to be used for collecting tags or strings into an array.
